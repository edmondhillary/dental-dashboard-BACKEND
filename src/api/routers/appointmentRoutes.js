import express from 'express';
import * as appointmentController from '../controllers/appointmentController.js';

const router = express.Router();

// router.post('/', appointmentController.createCita);
// router.get('/', appointmentController.getAllCitas);
// router.get('/:id', appointmentController.getCitaById);
// router.put('/:id', appointmentController.updateCitaById);
// router.delete('/:id', appointmentController.deleteCitaById);

export default router;
